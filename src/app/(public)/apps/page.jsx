import AppsPage from '@/pages/AppsPage';

export const metadata = {
  title: 'All Apps - AppInfoHub',
  description:
    'Explore the full AppInfoHub app library, including APK downloads, trusted app reviews, and top app recommendations for every category.',
};

export default function Apps() {
  return <AppsPage />;
}

